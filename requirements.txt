# Dependances directes du backend PoolPay.
# Les dependances transitives (asgiref, sqlparse, tablib, jsonschema...) sont
# resolues par pip : inutile de les figer ici.

# --- Coeur Django ---
Django==5.0.6
gunicorn==23.0.0
whitenoise==6.7.0

# --- Base de donnees (Render fournit DATABASE_URL) ---
dj-database-url==2.2.0
psycopg2-binary==2.9.9

# --- API REST ---
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
PyJWT==2.8.0
drf-spectacular==0.27.2
django-cors-headers==4.9.0

# --- Administration ---
django-jazzmin==3.0.0
django-extensions==3.2.3
django-import-export==4.0.6

# --- Utilitaires metier ---
shortuuid==1.0.13
Pillow==10.3.0

# Magasin de CA pour les appels HTTPS sortants (API Agharina).
certifi==2026.7.22
